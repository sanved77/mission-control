/** Primary UI font (bundled via @fontsource/cascadia-code in main.tsx) */
export const APP_FONT_FAMILY = '"Cascadia Code", monospace'

/** Default weight for body / paragraph content (theme + base styles) */
export const APP_CONTENT_FONT_WEIGHT = 300
